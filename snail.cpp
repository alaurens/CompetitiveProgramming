#include <algorithm>
#include <cstdio>
#include <cmath>
#include <cstring>
#include <bitset>

using namespace std;
int main(){
    int n;
    while(scanf("%d",&n) != EOF)
    {
        size_t si = n;
        vector<bool> bagobits;
        int a,b;
        scanf("%d",&a);
        while(n--){
            scanf("%d",&b);
            bagobits[(abs(a-b)]=true;

        }
    }
}
